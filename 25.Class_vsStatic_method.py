class Item:
    @staticmethod
    def is_integer():
        '''
            This should do something that has a relationship with the class, 
            but not something tha tmust be uniques per instance.
        '''
    def instantiate_from_something(cls):
        '''
        This should also do something that has arelationship wiht the 
        calss, but ussally, those are used to manipulate different structuress of
        the data to instantiate objects, like we have done with the CVS
        '''

# Never call the static and class methods from class instances
